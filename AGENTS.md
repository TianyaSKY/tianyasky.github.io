# 项目协作规范

## 适用范围与优先级

- 本文件适用于整个仓库；子目录中更具体的 `AGENTS.md` 优先。
- 开发者针对当前任务的明确说明优先于本文件。
- 修改前确认改动范围、当前分支、工作区未提交修改，并阅读相关实现与历史约定。
- 发现规范与现有实现、兼容性或部署要求冲突时，先定位影响并说明方案与风险；不得借当前任务擅自重写无关模块。

## 分层与数据边界

适用于包含后端或前端应用的未来项目：

- API 层负责 HTTP、鉴权、请求解析和将业务异常映射为响应；业务逻辑进入 Service 层。
- Service 层不依赖 `Request`、`Response`、`HTTPException` 或前端组件；它负责业务规则、流程编排、事务、重试与补偿。
- Repository 负责数据访问，Client 负责外部服务调用；两者不得返回 HTTP 响应。
- 所有带请求体的写接口使用 Pydantic `BaseModel`（模型按领域放在 `api/schemas/`）；完成校验后尽快转换为 `@dataclass` 的业务参数。Service 的参数和返回值使用明确类型，不以万能 `dict` 或 `**kwargs` 传递。
- 简单 Query、Path 参数使用框架原生约束。字段格式、范围和必填由 Pydantic 完成；业务规则由 Service 完成。
- 前端表单与关键请求体使用 Zod 校验。前端校验改善体验，后端校验才是权威；两端规则须保持语义一致。

## 异常与日志

- 使用清晰的业务异常类型；API 层统一映射为 HTTP 状态码和响应格式。
- 禁止裸 `except:`、捕获后静默忽略、以 `None` 表示所有失败，或向用户暴露完整异常堆栈。
- Python 运行日志统一使用 Loguru：`from loguru import logger`。业务代码不新增 `print()` 或标准库 `logging`，除非是 CLI 协议、安装/运维脚本、测试或框架兼容要求。
- 日志使用 Loguru 的 `{}` 占位符；在开始、完成、重试、降级和外部调用失败时记录必要上下文（如任务、用户、请求目标、次数和错误原因）。
- 不得无理由删除诊断日志、异常堆栈或开发者要求保留的注释。注释、文档字符串和内部日志默认使用简体中文；标识符和外部协议字段保持英文原样。

## 配置与敏感信息

- 不得自行假设现有密码、Token、Cookie、测试凭据或连接配置必须删除、改为环境变量或加入 `.gitignore`。
- 即使信息允许入库，也不得未经授权复制到公开仓库、公开文档、Issue、PR 或无关第三方服务。
- 不确定某项信息是否可提交或传播时，先向开发者确认。

## Git 工作流

开始任务前检查当前分支、`git status`、相关实现及历史约定，仅修改当前任务涉及的文件。

**默认情况下，每个独立任务必须创建新的 Git worktree，并在该 worktree 中创建对应的语义化分支进行开发。** 除开发者明确要求外，不得直接在当前工作目录使用 `git checkout` 或 `git switch` 开始新任务。

分支使用语义化命名，例如：`feat/`、`fix/`、`hotfix/`、`refactor/`、`docs/`、`chore/`、`investigate/`。

多人或多 Agent 并行开发时，每项任务必须拥有独立的 worktree 和分支，不得共享同一工作目录。

开发者指定已知正常版本时，应基于该版本创建新的 worktree 与 `investigate/<topic>` 分支进行对比分析，不得直接修改该版本。

未经开发者授权，禁止执行会丢失修改或改写历史的操作，包括但不限于：`git reset --hard`、`git clean -fd[x]`、`git restore .`、强制推送、删除分支或删除 worktree。不得使用 `git add .` 提交未检查的文件。

多 Agent 并行开发时，必须由主 Agent 先为每个子任务创建独立 worktree 和分支，再将对应路径分配给子 Agent。禁止多个 Agent 共用当前工作目录，也禁止先启动 Agent 再让其自行创建 worktree。子 Agent 开始修改前必须确认当前路径和分支与分配一致，否则停止执行并报告。

## 交付

完成任务时说明：修改摘要、主要文件、实际验证结果、未执行验证及原因、已知风险或后续事项。修改公共接口时同步更新调用方和相关文档。
